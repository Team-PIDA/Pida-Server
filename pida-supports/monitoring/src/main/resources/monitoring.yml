management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  health:
    defaults:
      enabled: false

sentry:
  dsn: ${SENTRY_DSN}
  traces-sample-rate: 1.0
  environment: ${SPRING_PROFILES_ACTIVE:local}